
--
pdk_deposit = {}

--针对打出的牌选出能压得牌
function pdk_deposit.get_can_out_cards(seatid )
	local cards = game_data.user_card[seatid]

	if game_data.last_out_type == algorithm.card_type.zhadan then

	end
end
